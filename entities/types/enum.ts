export enum Role {
    ADMIN = "admin",
    CUSTOMER = "customer"
}

export enum SenderType {
    USER = "user",
    BOT = "bot"
}

export enum Status {
    PENDING = "pending",
    ACCEPTED = "accepted",
    DELIVERED = "delivered",
    CANCELLED = "cancelled",
    PAID = "paid",
    UNPAID = "unpaid"
}
